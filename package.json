{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "yarn start-fire",
    "start-fire": "cp fire.env .env && react-scripts start",
    "start-water": "cp water.env .env && react-scripts start",
    "start-grass": "cp grass.env .env && react-scripts start",
    "start-dragon": "cp dragon.env .env && react-scripts start",
    "start-dark": "cp dark.env .env && react-scripts start",
    "build": "react-scripts build && cp water.env build/.env",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
